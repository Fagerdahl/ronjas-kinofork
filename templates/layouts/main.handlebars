<!--HTML structure with placeholders for dynamic data-->
<!doctype html>
<html>
  <head>
    <meta charset='utf-8' />
    <meta http-equiv='X-UA-Compatible' content='IE=edge' />
       <title>{{title}}</title>
    <meta name='viewport' content='width=device-width, initial-scale=1' />
    <link rel="stylesheet"  type="text/css" media="screen" href="/static/dist/styles.css" />
 <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&display=swap" rel="stylesheet" />


  </head>
  <body>
    <header class='header'>
      <div class='header__logo'>
        <h1 class='header__logo-title'>{{logoTitle}}</h1>
      </div>
      
      <main class='main'>
        <!-- Inject dynamic content (Moviepage) -->
        {{{body}}}
 
      </main>

    </header>

    <footer class='footer'>
      <p>{{footerText}}</p>
    </footer>

  </body>
</html>