
<!DOCTYPE html>
<html lang="sv">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{movie.title}}</title>
</head>
<body>
  <h1>{{movie.title}}</h1>
  <img src="{{movie.image}}" alt="{{movie.title}}" />
  <p>{{movie.intro}}</p>
  <a href="/">Backa tillbaka</a>
</body>
</html>
